# PDF.js

The Workbench documentation reader uses the prebuilt browser modules from
Mozilla PDF.js 5.6.205 (`pdfjs-dist`). PDF.js is licensed under Apache-2.0;
the upstream license is included as `LICENSE.txt`.

Only the viewer runtime and worker are bundled. Workbench supplies its own
accessible reader controls and keeps the reviewed PDF files authoritative.
