import importlib.util
import re
import tomllib
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]


class FrontendContractTests(unittest.TestCase):
    def test_hypercube_discovery_and_case_exports_are_manual_and_accessible(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn('data-hypercube-subpage="hypercubeExportPage"', markup)
        self.assertIn('aria-describedby="hypercubeDiscoveryHelp"', markup)
        self.assertIn("An 81-case regional Hypercube scan is often around 10 minutes", markup)
        self.assertNotIn('id="openHypercubeCompare"', markup)
        self.assertNotIn('id="compareHypercubePair"', markup)
        self.assertNotIn('id="viewHypercubeRows"', markup)
        self.assertIn('id="openHypercubeMap"', markup)
        self.assertIn("function queueHypercubeCaseExports()", source)
        self.assertIn("view.selected.size", source)
        self.assertIn("hypercube-case-zip", source)
        self.assertNotIn("_hypercube_index_loop", server)
        self.assertIn('/api/hypercube-exports/start', server)
        self.assertIn('/api/hypercube-exports/download', server)
        self.assertIn("self.diagnostics.record_app_error", server)
        self.assertIn('id="hypercubeExportActivity"', markup)
        self.assertIn('id="hypercubeOutputRankingPane"', markup)
        self.assertIn("function renderHypercubeOutputRanking()", source)
        self.assertIn("function renderHypercubeExportActivity", source)
        self.assertNotIn("Geographic spread", markup)

    def test_hypercube_export_checkboxes_have_a_full_size_target(self):
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('.hypercube-export-row input[type="checkbox"]{width:20px;height:20px', styles)

    def test_package_preview_prioritizes_plain_language_information(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn('id="packagePreviewDescription"', markup)
        self.assertIn('id="packagePreviewContents"', markup)
        self.assertIn("Package checked and ready to install", markup)
        self.assertIn('<summary>Technical details</summary>', markup)
        self.assertIn("preview.description||'No package description was provided.'", source)
        self.assertIn(".package-preview-technical", styles)
        self.assertIn('manifest.get("compatibilitySummary", "Validated for this Workbench package format")', server)

    def test_opt_in_update_checking_is_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn('id="settingsUpdates"', markup)
        self.assertIn('id="automaticUpdateChecks"', markup)
        self.assertIn('id="checkUpdatesNow"', markup)
        self.assertIn('id="settingsUpdateIndicator"', markup)
        self.assertIn('post("/api/updates/check"', source)
        self.assertIn("updateCheckSettingsFromControls", source)
        self.assertIn(".update-status-card", styles)
        self.assertIn('install_required:"Not installed"', source)
        self.assertIn('item.status==="install_required"?"Install Runtime":"Install Runtime Update"', source)
        self.assertIn(".update-status-card.install_required", styles)
        self.assertIn('parsed.path == "/api/updates/status"', server)
        self.assertIn('parsed.path == "/api/updates/check"', server)

    def test_region_selection_asset_recovery_and_workspace_repair_are_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="clearBuiltRegionSelection"', source)
        self.assertIn("state.pendingProjectSetup = null", source)
        self.assertIn("if (!pendingLibrary || !pendingTemplate)", source)
        self.assertIn("Built region package is inconsistent", source)
        self.assertIn('"input-library":"Input Library"', source)
        self.assertIn('"model-template":"Model package"', source)
        self.assertIn(".asset-row-actions", styles)
        self.assertRegex(styles, r"\.asset-row-actions\s*\{[^}]*gap:\s*10px")
        self.assertIn("This does not restore removed assets or rebuild regional packages", markup)
        self.assertIn('const button=event.currentTarget;setBusy(button,true,"Repairing…")', source)
        self.assertIn('finally{setBusy(button,false)}', source)
        self.assertIn('await openSettings("settingsReset")', source)
        self.assertIn("No settings save is required.", source)

    def test_project_copy_hypercube_and_safety_disclosure_are_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn('data-page="hypercubePage"', markup)
        self.assertIn('data-hypercube-subpage="hypercubeBuildPage"', markup)
        self.assertIn('data-hypercube-subpage="hypercubeAnalyzePage"', markup)
        self.assertIn('id="hypercubeSafetyDialog"', markup)
        self.assertIn('Hypercubes are safe to run on a laptop.', markup)
        self.assertIn('I understand — open Hypercube', markup)
        self.assertIn('id="projectCopyDialog"', markup)
        self.assertIn('id="projectCopyResultEstimate"', markup)
        self.assertIn('id="scenarioCopyResultEstimate"', markup)
        self.assertIn('id="copyCompleteDialog"', markup)
        self.assertNotIn('id="projectCopyIncludeResults"', markup)
        self.assertNotIn('id="scenarioCopyIncludeResults"', markup)
        self.assertIn('id="scenarioCopyDialog"', markup)
        self.assertIn('id="hypercubeAxes"', markup)
        self.assertIn('id="hypercubePreview"', markup)
        self.assertIn('id="hypercubeScope"', markup)
        self.assertIn('id="hypercubeLocationPopover"', markup)
        self.assertNotIn('id="saveHypercube"', markup)
        self.assertIn('id="retryHypercubeSave"', markup)
        self.assertIn('id="hypercubeSaveStatus"', markup)
        self.assertNotIn('id="hypercubeUnsavedDialog"', markup)
        self.assertNotIn('id="hypercubeName"', markup)
        self.assertLess(markup.index('id="hypercubeAxes"'), markup.index('id="hypercubeScope"'))
        self.assertIn("function renderHypercubeSetup()", source)
        self.assertIn("function generateHypercube()", source)
        self.assertIn("function hypercubeDiagram(preview)", source)
        self.assertIn("async function saveHypercubeDraft", source)
        self.assertIn("async function flushHypercubeAutosave()", source)
        self.assertIn("scheduleHypercubeAutosave", source)
        self.assertIn('document.addEventListener("pointerdown",(event)=>{if(!eventInsideLocationPopover(event))closeLocationPopovers();},true)', source)
        self.assertIn("hypercubeLocationPopoverOpen", source)
        self.assertIn("hypercubeResourceEstimateMarkup", source)
        self.assertIn("81-case Windows planning example", source)
        self.assertIn("One active slot", source)
        self.assertIn("async function ensureHypercubeFile(filename)", source)
        self.assertIn('post("/api/projects/hypercubes/draft"', source)
        self.assertIn('role="img"', source)
        self.assertIn("Example combinations", source)
        self.assertIn("All matching rows", source)
        self.assertIn("hypercubeSafetyAcknowledged: false", source)
        self.assertIn('post("/api/projects/copy/start"', source)
        self.assertIn('post("/api/projects/variations/copy/start"', source)
        self.assertIn("scenarioCopyBlockingStates", source)
        self.assertIn("activeScenarioCopyJobs", source)
        self.assertIn("scenarioCopyBlockedMessage", source)
        self.assertIn("activeProjectCopyJobs", source)
        self.assertIn("destinationCopyBlockedMessage", source)
        self.assertIn("Please wait until every run in this project has finished or been stopped.", source)
        self.assertIn("Cannot copy while selected scenario runs are in progress", source)
        self.assertIn("Cannot copy this project while its runs are in progress", source)
        self.assertIn("data-copy-disabled-reason", source)
        self.assertIn('post("/api/projects/results/unlink"', source)
        self.assertIn('request(`/api/projects/variations/delete-impact', source)
        self.assertIn('data-remove-project-scenario', source)
        self.assertIn("data-expand-project", source)
        self.assertIn('post("/api/projects/hypercubes/preview"', source)
        self.assertIn("data-run-hypercube", source)
        self.assertIn(".hypercube-axis-row", styles)
        self.assertIn(".hypercube-safety-dialog", styles)
        self.assertIn(".project-card-details", styles)
        self.assertIn(".project-card-chevron", styles)
        self.assertIn("fill:currentColor", styles)
        self.assertIn(".automatic-summary", styles)
        self.assertIn(".changed-row-table", styles)
        self.assertIn("Changed-row audit", source)
        self.assertIn('id="scenarioCopyDestinationStatus"', markup)
        self.assertNotIn('id="scenarioCopyDialog" class="wide-dialog taskbar-safe-dialog"', markup)
        self.assertIn('parsed.path == "/api/projects/copy"', server)
        self.assertIn('parsed.path == "/api/projects/variations/copy"', server)
        self.assertIn('parsed.path == "/api/projects/results/unlink"', server)
        self.assertIn('parsed.path == "/api/projects/hypercubes/start"', server)
        self.assertIn('parsed.path == "/api/projects/hypercubes/draft"', server)
        self.assertIn('parsed.path == "/api/projects/variations/delete-impact"', server)
        self.assertNotIn('parsed.path == "/api/projects/parameter-sweeps/start"', server)

    def test_windows_hypercube_guidance_is_safe_serialized_and_advisory(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("Hypercubes are safe to run on a laptop", markup)
        self.assertIn("less than 16 GB of RAM", markup)
        self.assertIn("This is guidance, not a block", markup)
        self.assertIn("81 serialized execution waves", markup)
        self.assertIn("parallel execution is not available in Windows 2.0", markup)
        self.assertNotIn("21 execution waves", markup)
        self.assertNotIn("four concurrent runs", markup.lower())
        self.assertIn("physicalMemoryBytes", source)
        self.assertIn("workspaceFreeBytes", source)
        self.assertIn("confirmHypercubeDiskPlan", source)

    def test_windows_native_resource_guidance_is_serialized(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="parallelMemoryGuide"', markup)
        self.assertNotIn('id="statewideMemoryGuide"', markup)
        self.assertIn('id="memoryLimitGuide"', markup)
        self.assertIn('id="runResourceGuide"', markup)
        self.assertIn("Queued — one native VisionEval case at a time", markup)
        self.assertIn('value="parallel" hidden disabled', markup)
        self.assertIn('id="autoStartDocker" type="checkbox">', markup)
        self.assertIn("Windows 2.0 serializes Standard and Hypercube work", markup)
        self.assertIn("MPO/regional models:", source)
        self.assertIn("2.5–3.5 GB per active run", source)
        self.assertNotIn("24–32 GB per active run (will be updated once tested)", source)
        self.assertNotIn("Run the first statewide baseline by itself", source)
        self.assertIn("Windows native runs are serialized", source)
        self.assertIn("does not affect containers already running", source)
        self.assertIn("function isStatewideProject", source)
        self.assertIn("function renderRunResourceGuide", source)
        self.assertIn(".warning-text", styles)
        self.assertNotIn("roughly 8 GB per run plus 8 GB for macOS", source)

    def test_hypercube_retention_compare_state_and_map_export_queue_are_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("map((item)=>Number(item.datastoreBytes))", source)
        self.assertIn("Datastores only · no optional full CSV trees", source)
        self.assertIn("Hypercube runs do not generate the optional full CSV tree", source)
        self.assertIn("Windows 2.0 safely executes one case at a time", source)
        self.assertNotIn("plan.retainExports?\"Full CSV exports retained\"", source)
        self.assertIn("function syncComparePairOptions()", source)
        self.assertIn("function captureCompareViewState()", source)
        self.assertNotIn('["changedOnly","directionalDeltas","findChangedOutputs","generateDashboard"]', source)
        self.assertIn("function syncDashboardGenerateAvailability()", source)
        self.assertIn("exportQueue: []", source)
        self.assertIn("function enqueueExport(label,run)", source)
        self.assertIn("function enqueueComparisonMapExport(kind)", source)
        self.assertIn("function snapshotComparisonMapVisual(format)", source)
        self.assertIn('id="mapExportStatus"', markup)
        self.assertIn('id="exportQueueStatus"', markup)
        self.assertIn('class="storage-cache-actions"', markup)
        self.assertIn('aria-describedby="comparisonCacheHelp"', markup)
        self.assertIn(".storage-cache-actions", styles)

    def test_compact_baseline_control_and_memory_retry_are_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('class="batch-from-baseline"', markup)
        self.assertIn('class="inline-check batch-baseline-toggle"', markup)
        self.assertIn('class="control-help"', markup)
        self.assertNotIn('class="notice guidance-notice" role="status" aria-live="polite" hidden></p>', markup)
        self.assertIn("Parallel runs share Docker memory.", source)
        self.assertIn("Settings → Resources", source)
        self.assertIn("function jobDisplayMessage(job)", source)
        self.assertIn("function chooseMemoryRetry(job)", source)
        self.assertIn("function retryRun(job)", source)
        self.assertIn("Retry Run…", source)
        self.assertIn("Open Resource Settings", source)
        self.assertIn(".run-resource-guide", styles)
        self.assertIn(".control-help-tooltip", styles)

    def test_macos_runtime_commands_use_the_verified_version_two_alias(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        alias = "local/visioneval:2.0.0-arm64"
        self.assertIn(alias, markup)
        self.assertNotIn("local/visioneval:ve-40-rc6-planrva-arm64", markup)
        self.assertIn("sha256:f6dba706e39bc403ad08c8fb27c2a8d69d74875de096475df3ac8311e1c13791", markup)

    def test_macos_release_workflow_uses_version_two_runtime_index(self):
        workflow = (ROOT.parent / ".github" / "workflows" / "release-macos.yml").read_text(encoding="utf-8")
        self.assertIn("release 2.0.0", workflow)
        self.assertIn("gh release download v2.0.0", workflow)
        self.assertIn("VisionEval-Workbench-v2.0.0-macos-arm64.dmg", workflow)
        self.assertNotIn("runtime-VE-40-RC7", workflow)
        self.assertNotIn("VisionEval-Workbench-v1.1.0-macos-arm64.dmg", workflow)

    def test_managed_runtime_install_and_platform_exclusive_guide_are_present(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        builder = (ROOT / "packaging" / "build_backend.py").read_text(encoding="utf-8")
        self.assertIn('id="onboardingInstallRuntime"', markup)
        self.assertIn('id="settingsInstallRuntime"', markup)
        self.assertIn("async function installAndSaveRuntime(", source)
        self.assertIn('post("/api/runtime/install/start", native?', source)
        self.assertIn('/api/runtime/install/status?id=', source)
        self.assertIn('/api/runtime/install/cancel', source)
        self.assertIn('parsed.path == "/api/runtime/install"', server)
        self.assertIn('parsed.path == "/api/runtime/install/start"', server)
        self.assertIn('parsed.path == "/api/runtime/install/status"', server)
        self.assertIn('data-runtime-platform="macos"', markup)
        self.assertIn('data-runtime-platform="windows"', markup)
        self.assertIn("runtime-platform:{candidate}:start", builder)
        self.assertIn("prunePlatformSpecificContent", source)
        self.assertIn("Install R 4.5.3 + VisionEval RC7", source)
        self.assertIn('id="onboardingDetectRuntime"', markup)
        self.assertIn('id="settingsDetectRuntime"', markup)

    def test_runtime_install_completion_has_persistent_shared_ui_state(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        for control in ("onboardingRuntimeStatus", "settingsRuntimeStatus"):
            self.assertRegex(
                markup,
                rf'id="{control}"[^>]*role="status"[^>]*aria-live="polite"[^>]*aria-atomic="true"',
            )
        self.assertIn("function runtimeSetupSnapshot()", source)
        self.assertIn("function renderRuntimeSetupControls()", source)
        self.assertIn('runtimeSetupPhase: "idle"', source)
        self.assertIn('hidden:verified', source)
        self.assertIn('label:verifyLabel', source)
        self.assertIn('verified ? "Verify again" : "Verify runtime"', source)
        self.assertIn('$("onboardingSkip").hidden = verified', source)
        self.assertIn('"Runtime installed, verified, and connected."', source)
        self.assertIn('state.runtimeSetupPhase = "failed"', source)
        self.assertIn('"Retry installation"', source)
        self.assertIn('nativeNotification("VisionEval runtime ready"', source)
        self.assertIn(".runtime-setup-status.success", styles)
        self.assertIn(".runtime-setup-status.error", styles)
        self.assertIn(".runtime-setup-status.progress", styles)

    def test_current_arm64_runtime_alias_is_not_migrated_as_legacy(self):
        host = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        migration = host.split("fn runtime_image_needs_migration", 1)[1].split("fn is_legacy_workspace", 1)[0]
        self.assertIn("LEGACY_RUNTIME_IMAGE | UNPATCHED_RC6_RUNTIME_IMAGE", migration)
        self.assertNotIn("ARM64_RUNTIME_IMAGE", migration)

    def test_packaged_app_uses_in_app_confirmation_dialogs(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        self.assertIn("function confirmWorkbench(", source)
        self.assertNotIn("window.confirm(", source)
        self.assertNotIn("window.prompt(", source)
        self.assertIn('id="confirmationDialog"', markup)
        self.assertIn('id="confirmationDialogPhrase"', markup)
        self.assertIn('requiredPhrase:confirmation', source)
        self.assertIn('phraseInput.value !== requiredPhrase', source)
        self.assertIn('"factory_reset_workspace"', permissions)
        self.assertIn('"reset_preferences"', permissions)

    def test_version_two_upgrade_guidance_is_non_destructive_and_acknowledged(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        host = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        self.assertIn('id="upgradeNoticeDialog"', markup)
        self.assertIn("No factory reset is required.", markup)
        self.assertIn('id="upgradeReviewAssets"', markup)
        self.assertIn('id="upgradeReadWhatsNew"', markup)
        self.assertIn('id="upgradeDismiss"', markup)
        self.assertIn('invoke("acknowledge_upgrade_notice")', source)
        self.assertIn('openSettings("settingsAssets")', source)
        self.assertIn('openDocumentationReader("whats-new"', source)
        self.assertIn("upgrade_notice_pending", host)
        self.assertIn("last_acknowledged_application_version", host)
        self.assertIn('"acknowledge_upgrade_notice"', permissions)
        self.assertNotIn("factory_reset_workspace", source.split("function finishUpgradeNotice", 1)[1].split("function closeWorkspaceMenus", 1)[0])

    def test_disabled_buttons_have_shared_explanations(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        setup = (ROOT / "desktop" / "src" / "index.html").read_text(encoding="utf-8")
        self.assertIn("function setButtonAvailability(", source)
        self.assertIn("function installDisabledButtonGuidance(", source)
        self.assertIn("dataset.disabledReason", source)
        self.assertIn("button:disabled", source)
        self.assertIn(".disabled-button-tooltip", styles)
        self.assertIn("function setButtonAvailability(", setup)
        self.assertIn("disabled-button-tooltip", setup)

    def test_create_defaults_to_setup_and_develop_preloads_the_region_map(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        develop_index = markup.index('data-create-subpage="createDevelop"')
        setup_index = markup.index('data-create-subpage="createSetup"')
        self.assertLess(develop_index, setup_index)
        self.assertIn('id="regionPackage"', markup)
        self.assertIn('id="regionSourceLibrary"', markup)
        self.assertIn('id="regionDefinition"', markup)
        self.assertIn('id="regionOutputOptions"', markup)
        self.assertIn("Output options", markup)
        self.assertIn('id="installRegionPackage"', markup)
        self.assertIn("No compatible asset package installed", markup)
        self.assertNotIn('id="templateSelect"', markup)
        self.assertNotIn('id="defaultTemplate"', markup)
        self.assertIn("state.data?.developSources", source)
        self.assertNotIn('id="regionAzones"', markup)
        self.assertNotIn('id="regionBzones"', markup)
        self.assertIn('id="previewRegionBuild"', markup)
        self.assertIn('id="viewRegionMap"', markup)
        self.assertIn('id="buildRegionAssets"', markup)
        self.assertIn('id="regionMapDialog"', markup)
        self.assertIn("/api/region-builder/reference", source)
        self.assertIn("/api/region-builder/packages", (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8"))
        self.assertIn("/api/region-builder/sources", source)
        self.assertIn("/api/region-builder/regions", source)
        self.assertIn("/api/region-builder/preview", source)
        self.assertIn("/api/region-builder/build", source)
        self.assertIn("/api/region-builder/map", source)
        self.assertIn("/api/region-builder/map/statewide", source)
        self.assertIn("/api/region-builder/geography-options", source)
        self.assertIn('id="regionGeographyDialog"', markup)
        self.assertIn('id="regionMapRegion"', markup)
        for control in ("zoomOutRegionMap", "zoomInRegionMap", "fitRegionMap", "resetRegionMap"):
            self.assertIn(f'id="{control}"', markup)
        for layer in ("mpos", "azones", "all-bzones", "selected-bzones", "included-boundary"):
            self.assertIn(f'data-region-map-layer="{layer}"', markup)
        for label in (
            "All MPO boundaries", "All Azones", "All Bzones", "Selected MPO Bzones",
            "Selected MPO boundary overlaps",
        ):
            self.assertIn(label, markup)
        self.assertIn("at least 1% but less than 99%", markup)
        self.assertIn("Included (substantially inside)", source)
        self.assertNotIn("inside or centered", source)
        self.assertNotIn('data-region-map-layer="excluded-boundary"', markup)
        self.assertNotIn("intersecting exclusions", source)
        self.assertIn('id="regionMapNameLabels" type="checkbox" checked', markup)
        self.assertIn('id="regionMapIdLabels" type="checkbox"> Zone ID labels', markup)
        self.assertIn('id="regionMapInspector"', markup)
        self.assertIn('id="closeRegionMapInspector"', markup)
        self.assertIn("function renderInstalledScopeSummary()", source)
        self.assertIn('$("regionOutputOptions").hidden = installed', source)
        self.assertIn('$("previewRegionBuild").hidden = installed', source)
        self.assertIn('$("buildRegionAssets").hidden = installed', source)
        self.assertIn('modelScoped ? "" : ordinary.map', source)
        self.assertIn('"View map (loading…)"', source)
        self.assertIn('mapButton.setAttribute("aria-busy", String(loading))', source)
        for control in (
            "regionMapFocus", "regionMapMpoLayerWrapper", "regionMapAzoneLayerWrapper",
            "regionMapBzoneLayerWrapper", "regionMapSelectedLayerWrapper", "regionMapBoundaryLayerWrapper",
        ):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn('group.style.display = regionMapLayerVisible(input.dataset.regionMapLayer) ? "" : "none"', source)
        self.assertNotIn("group.hidden = !input.checked", source)
        self.assertIn('Boolean(input?.checked && !input.closest("[hidden]"))', source)
        self.assertIn("function regionMapVisibleBzoneIds()", source)
        self.assertIn('if (regionMapLayerVisible("all-bzones")) return null', source)
        self.assertIn('if (state.regionMapSelectedFeature && !regionMapSelectedFeatureVisible()) clearRegionMapInspector()', source)
        self.assertIn('aria-hidden="true" data-map-group="all-bzones"', source)
        self.assertIn("state.regionMapSelectedRegionId = regionId", source)
        self.assertIn("regionMapClientPoint", source)
        self.assertIn("regionMapHitFeature", source)
        self.assertIn("updateRegionMapIdLabels", source)
        self.assertIn("labels.length >= (useBzones ? 45 : 90)", source)
        self.assertIn("zoom >= 40", source)
        self.assertIn("zoom >= 1.75", source)
        self.assertIn("regionMapOccupyLabel", source)
        self.assertIn("bzoneMemberships", source)
        self.assertNotIn("Intersects but excluded from", source)
        self.assertIn("Included in MPOs", source)
        self.assertIn("Explored MPO official ID", source)
        self.assertIn("regionMapFeaturesView(reviewFeatures", source)
        self.assertNotIn('state.regionBuilderRegionId = $("regionMapRegion").value', source)
        self.assertIn("may extend well beyond an official MPO study area", markup)
        self.assertIn("Data sources", source)
        self.assertIn('switchCreateSubpage("createSetup", false)', source)
        self.assertIn("preloadRegionMapGeometry", source)
        self.assertIn('regionMapLoadState: "idle"', source)
        for status in ("Preparing map", "Map ready", "Cached locally"):
            self.assertIn(status, source)

    def test_editor_shows_loaded_rows_before_location_selection(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn(
            "state.editorFileName = filename; state.csv = csvPayload; state.editorGeography = geography; state.editorSelectedLocations.clear();",
            source,
        )
        self.assertIn(
            'if ($("editorLocationField").value === "all" || !selected.length) return true;',
            source,
        )
        self.assertIn(
            'if ($("editorLocationField").value !== "all" && !state.editorSelectedLocations.size)',
            source,
        )

    def test_editor_filter_controls_have_equal_widths(self):
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("grid-template-columns: repeat(10, minmax(0, 1fr));", styles)
        self.assertIn(
            '"type type search search year year operation operation value value"',
            styles,
        )

    def test_editor_sidebar_is_wider_without_pushing_main_pane_offscreen(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("--scenario-sidebar-width: 340px", styles)
        self.assertIn("max-width: 100%", styles)
        self.assertIn("overflow: hidden", styles)
        self.assertIn("Math.max(320", source)

    def test_queue_drag_does_not_late_capture_pointer_in_webview(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        queue_drag = source[source.index("function enableQueueDragging"):source.index("function consoleBatchJobs")]
        self.assertNotIn("setPointerCapture", queue_drag)

    def test_editor_distinguishes_saved_baseline_changes_from_unsaved_preview(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("editorBaselineRows", source)
        self.assertIn("baselineEditorFileUrl", source)
        self.assertIn('saved ? "saved-change-cell"', source)
        self.assertIn('unsaved ? "changed-cell"', source)
        self.assertIn("Saved scenario change from baseline", source)
        self.assertIn("Saved change from baseline", markup)
        self.assertIn("Unsaved direct edit", markup)
        self.assertIn(".editor-table td.saved-change-cell", styles)

    def test_batch_draft_is_scenario_scoped_and_restored(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        self.assertIn(
            "if (!batchSessionMatches(scenario.id)) resetBatchDraft(scenario.id);",
            source,
        )
        self.assertIn("function batchSessionMatches(", source)
        self.assertIn("function clearBatchDraftState(", source)
        self.assertIn('loadEditorDraft("batch") || savedBatchDraft(scenario)', source)
        self.assertIn('storeEditorDraft("batch", batchDraftFromControls())', source)
        self.assertIn(
            'clearBatchDraftState("");',
            source,
        )
        self.assertIn(
            'state.editorVariationId = scenarioId; state.editorMode = "file"; clearEditorFile();',
            source,
        )
        self.assertIn('fileSignature !== currentFileSignature', source)
        self.assertIn('sessionOwner !== state.batchSessionOwner', source)
        self.assertIn('input.checked = state.batchSelectedFiles.has(input.dataset.batchFile);', source)
        self.assertIn('box.checked = state.batchSelectedColumns.get(box.dataset.batchColumnFile)?.has(box.dataset.batchColumn) || false;', source)
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn(
            ".batch-location-type, .batch-location-search, .batch-year, .batch-operation, .batch-value { grid-column: span 2; }",
            styles,
        )
        self.assertIn('.batch-editor-grid > label { min-width: 0; }', styles)

    def test_editor_drafts_restore_saved_selections_and_mixed_states(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('EDITOR_DRAFT_STORAGE_KEY = "visioneval-editor-drafts-v1"', source)
        self.assertIn("function pruneEditorDrafts()", source)
        self.assertIn('loadEditorDraft("file", filename) || savedFileDraft', source)
        self.assertIn('meaningfulEditOperations(overlay.editOperations, "batch")', source)
        self.assertIn('MIXED_EDITOR_VALUE = "__mixed__"', source)
        self.assertIn('"Mixed saved changes"', source)
        self.assertIn('"Mixed saved scopes"', source)
        self.assertIn("Saved edits use different operations or change amounts.", source)
        self.assertIn("function savedDraftGuidance", source)
        self.assertIn("scopeUnavailable", source)
        self.assertIn("Choose location type", source)
        self.assertIn('operation === MIXED_EDITOR_VALUE', source)
        self.assertIn('type === MIXED_EDITOR_VALUE', source)
        self.assertIn('id="clearEditorSelections"', markup)
        self.assertIn('id="clearBatchSelections"', markup)
        self.assertIn('id="editorSavedScopeSummary"', markup)
        self.assertIn('id="batchSavedScopeSummary"', markup)
        self.assertIn(".saved-scope-summary", styles)
        self.assertIn('height: 40px; min-height: 40px;', styles)
        self.assertIn('resetBatchDraft(state.editorVariationId, true);', source)
        self.assertIn('calculatedValue(next, "batch", csvPayload, column)', source)
        self.assertIn('const priorYear = $("batchYear").value || $("batchYear").dataset.draftYear || "";', source)
        self.assertIn('<option value="">Choose operation</option>', markup)

    def test_editor_exposes_authoritative_validation_and_atomic_batch_controls(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        for control in ("editorCategoryValue", "batchCategoryValue", "editorShareGroup", "batchShareGroup", "editorValidationStatus"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn("function clientValidationErrors", source)
        self.assertIn("function syncEditorEditMode", source)
        self.assertIn("function syncBatchEditMode", source)
        self.assertIn('post("/api/overlays/batch"', source)
        self.assertIn("All rows — no year field", source)
        self.assertIn("function hypercubeAxisColumns", source)
        self.assertNotIn("next=Math.min(1,next)", source)
        self.assertIn(".editor-table .categorical-cell", styles)
        self.assertIn(".editor-table td.invalid-cell", styles)

    def test_windows_dialogs_use_native_work_area_and_explicit_safe_bounds(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        self.assertIn("fn window_layout_metrics", rust)
        self.assertIn("monitor.work_area()", rust)
        self.assertIn('invoke("window_layout_metrics")', source)
        self.assertIn("metrics.client.width / Math.max(1, root.clientWidth)", source)
        self.assertIn("offsetLeft", source)
        self.assertIn("ResizeObserver", source)
        self.assertIn('window.addEventListener("workbench-native-layout-change", syncWorkbenchViewport)', source)
        self.assertIn("tauri::WindowEvent::ScaleFactorChanged", rust)
        self.assertIn("WORKBENCH_DIALOG_GAP = 24", source)
        self.assertIn('id="settingsDialogResizer"', markup)
        self.assertIn("ArrowRight", source)
        self.assertIn("Home", source)
        self.assertIn(".workbench-large-dialog", styles)
        self.assertIn('"window_layout_metrics"', permissions)
        self.assertNotIn("calc(var(--workbench-viewport-height) - 112px)", styles)

    def test_editor_guards_overlapping_operations_and_exposes_provenance_tooltips(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("function rowsDifferAt(", source)
        self.assertIn("function chooseOverlappingOperation(", source)
        self.assertIn("already adjusted from baseline", source)
        self.assertIn('accept.textContent = "Apply additional operation"', source)
        self.assertIn('alternative.textContent = "From baseline"', source)
        self.assertIn('id="batchFromBaseline"', markup)
        self.assertIn('const basis = requestedFromBaseline ? "baseline"', source)
        self.assertIn('dialog.returnValue === "confirm" ? "current"', source)
        self.assertIn("operationsWithBaselineOverride", source)
        self.assertIn('operationId:newOperationId("single-file")', source)
        self.assertIn('operationId,source:"batch"', source)
        self.assertIn("state.batchBaselineFiles", source)
        self.assertIn("rounding:operationRounding", source)
        self.assertIn('class="edit-source-tooltip" role="tooltip"', source)
        self.assertIn("scenarioEditSource(scenario)", source)
        self.assertIn(".edit-source-marker:hover .edit-source-tooltip", styles)
        self.assertIn(".edit-source-marker:focus-visible .edit-source-tooltip", styles)

    def test_setup_baseline_and_editor_copy_language_are_explicit(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="existingBaselineChoice"', markup)
        self.assertIn('id="existingBaselineHelp"', markup)
        self.assertIn("No compatible completed baseline", source)
        self.assertIn('$("existingBaselineStrategy").disabled=!baselines.length', source)
        self.assertIn("Copy Scenario", markup)
        self.assertNotIn("Duplicate Scenario", markup)
        self.assertIn(".setup-field-group", styles)
        self.assertIn(".review-toolbar input", styles)
        self.assertIn('.hypercube-shell input:not([type="checkbox"]):not([type="radio"])', styles)

    def test_single_file_calculations_save_atomically_and_new_file_clears_controls(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("Apply and Save Change", markup)
        self.assertIn("Save Direct Edits", markup)
        self.assertIn("function clearEditorFile()", source)
        self.assertIn('"editorLocationField","editorLocationSearch","editorYear"', source)
        self.assertIn('"applyEditorChange","clearEditorSelections"', source)
        self.assertIn('await post("/api/overlays"', source)
        self.assertIn("Save or revert direct table edits before applying a calculated change.", source)

    def test_primary_navigation_is_session_stateful_and_review_selects_projects(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        self.assertIn('activePrimaryPage: "explorePage"', source)
        self.assertIn('activeExploreSubpage: "exploreLibraryPage"', source)
        self.assertIn('activeCreateSubpage: "createSetup"', source)
        self.assertIn('activeCompareSubpage: "compareData"', source)
        self.assertNotIn('data-subpage="loadData"', markup)
        self.assertIn('id="compareReference"', markup)
        self.assertIn('id="compareComparison"', markup)
        self.assertIn("primaryPageScroll: new Map", source)
        self.assertIn("function restorePrimaryPageScroll(", source)
        self.assertIn("if (outgoing === pageId) {", source)
        self.assertIn("if (!restoreScroll) window.scrollTo({left:0, top:0});", source)
        self.assertIn("switchCreateSubpage(state.activeCreateSubpage, false)", source)
        self.assertNotIn('if (pageId === "createPage") switchCreateSubpage("createSetup"', source)
        self.assertNotIn('if (pageId === "runPage") setRunHistoryHidden(false)', source)
        navigation_state = source[source.index("activePrimaryPage:"):source.index("expandedProjectIds:")]
        self.assertNotIn("localStorage", navigation_state)
        self.assertNotIn("sessionStorage", navigation_state)
        self.assertIn('id="reviewProjectSelect"', markup)
        self.assertIn('$("reviewProjectSelect").addEventListener("change"', source)
        self.assertIn('$("reviewProjectSelect").value = state.selectedProject?.id || "";', source)

    def test_hypercube_eta_and_compact_run_history_are_wired(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('<script src="hypercube-runtime-summary.js"></script>', markup)
        self.assertLess(markup.index('src="hypercube-runtime-summary.js"'), markup.index('src="app.js"'))
        self.assertIn("function hypercubeEtaEstimate(project", source)
        self.assertIn("function hypercubeRunAggregate(project)", source)
        self.assertIn("slice(0,25)", source)
        self.assertIn("measured median of", source)
        self.assertIn("11-minute Apple Silicon planning value", source)
        self.assertIn("Estimated remaining", source)
        self.assertIn("data-open-hypercube-run", source)
        self.assertIn("function openHypercubeRunProject(projectId)", source)
        self.assertIn("ordinaryJobs", source)
        self.assertIn("hypercubeProjectIds", source)
        self.assertIn(".hypercube-job-summary", styles)

    def test_hypercube_run_notifications_are_project_scoped(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("hypercubeNotificationSnapshot: null", source)
        self.assertIn("if(hypercubeProjects.has(job.projectId))continue;", source)
        self.assertIn("if(!previous?.unresolved||unresolved.length)continue;", source)
        self.assertIn("Hypercube completed`,`${total} runs finished", source)
        self.assertIn("Hypercube finished with issues", source)
        self.assertIn("Hypercube stopped", source)

    def test_hypercube_long_operations_send_completion_notifications(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("nativeNotification('Hypercube analysis ready'", source)
        self.assertIn("nativeNotification('Find All Changes complete'", source)
        self.assertIn('nativeNotification("Hypercube scenarios ready"', source)
        self.assertIn("function enqueueHypercubeExport(label,run)", source)
        self.assertIn("export ${batch.failed?'finished with issues':'complete'}", source)
        self.assertNotIn("if(state.activePrimaryPage!=='hypercubePage'||state.activeHypercubeSubpage!=='hypercubeAnalyzePage')nativeNotification('Find All Changes complete'", source)
        self.assertNotIn("if(state.activePrimaryPage!=='hypercubePage'||state.activeHypercubeSubpage!=='hypercubeAnalyzePage')nativeNotification('Hypercube analysis ready'", source)

    def test_mixed_edit_source_emblem_uses_one_compact_purple_m(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn("edit-source-mixed-letter", source)
        self.assertIn('aria-hidden="true">M</span>', source)
        self.assertIn("Batch and single-file changes", source)
        self.assertIn("width:24px; min-width:24px; height:24px", styles)
        self.assertIn("#7c3aed", styles)
        self.assertNotIn("edit-source-mixed-batch", source)
        self.assertNotIn("edit-source-mixed-single", source)
        self.assertNotIn(".edit-source-mixed { width:30px", styles)

    def test_hypercube_projects_are_dedicated_and_keep_year_selection(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        self.assertNotIn('name="projectType"', markup)
        self.assertNotIn('name="projectType" value="hypercube"', markup)
        self.assertIn('id="hypercubeProjectForm"', markup)
        self.assertIn('id="hypercubeYear"', markup)
        self.assertIn("which dated CSV rows receive matrix changes", markup)
        self.assertIn('const projectType = "standard"', source)
        self.assertIn("filter((project)=>project.projectType==='hypercube')", source)
        self.assertIn('function hypercubeProjectEditable()', source)
        self.assertIn('replaceExisting:Boolean(hypercubeProject()?.hypercubes?.length)', source)
        self.assertIn('hypercubeProject?"Open Hypercube":"Open in Editor"', source)
        self.assertIn('id="scenarioRemovalDialog"', markup)
        self.assertIn('aria-label="${escapeHtml(removeLabel)}"', source)
        self.assertIn("Each combination changes every matching row; no MPO aggregation or location dimension is added.", source)
        self.assertNotIn('id="hypercubeName"', markup)

    def test_latest_workbench_ux_corrections_are_wired(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertNotIn("Project type", markup)
        self.assertIn("Choose the starting values", markup)
        self.assertIn("Start this batch from the untouched baseline", markup)
        self.assertIn("Current scenario values — apply this operation on top of changes already saved.", source)
        self.assertIn("Untouched baseline values — replace earlier changes within the selected files, columns, year, and locations.", source)
        self.assertIn("function nextScenarioName", source)
        self.assertIn("const form=event.currentTarget", source)
        self.assertNotIn("event.currentTarget.reset()", source)
        self.assertIn("Clear history will be available after all running and queued jobs finish.", source)
        self.assertIn(".run-history-actions", styles)
        self.assertIn("height:32px", styles)
        self.assertIn("height:40px", styles)
        self.assertIn("--comparison-map-decrease", styles)
        self.assertIn("comparisonMapMareaFeatures", source)
        self.assertIn("Richmond UZA", source)
        self.assertIn("Non-UZA", source)

    def test_batch_editor_has_one_guarded_scenario_scoped_implementation(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertEqual(source.count("function renderBatchFiles("), 1)
        self.assertEqual(source.count("async function renderBatchColumns("), 1)
        self.assertIn("function resetBatchDraft(", source)
        self.assertIn("requestId !== state.batchColumnsRequestId", source)
        self.assertIn("state.batchSelectedFiles", source)
        self.assertIn("state.batchSelectedColumns", source)

    def test_editor_omits_summaries_and_review_explains_rounding_no_ops(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertNotIn('id="batchSelectionSummary"', markup)
        self.assertNotIn("function renderBatchSelectionSummary()", source)
        self.assertNotIn(".batch-selection-summary", styles)
        self.assertIn("Effective differences:", source)
        self.assertIn("group.locations?.effectiveText", source)

    def test_notes_autosave_independently_from_csv_changes(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="editorNotesTitle"', markup)
        self.assertIn('id="scenarioNoteField"', markup)
        self.assertIn('id="scenarioNoteStatus"', markup)
        self.assertIn('id="fileNoteStatus"', markup)
        self.assertIn('aria-live="polite"', markup)
        self.assertNotIn('id="saveScenarioNote"', markup)
        self.assertIn('$("scenarioNoteField").hidden = !showScenarioNote;', source)
        self.assertIn('$("fileNoteField").hidden = !showFileNote;', source)
        self.assertIn('showScenarioNote ? "Scenario note" : "File note"', source)
        self.assertIn('Choose an input file to add or edit its file note.', source)
        self.assertIn('.editor-note-field[hidden] { display:none; }', styles)
        self.assertIn("setTimeout(() => { tracker.timer = null; saveNoteNow(kind); }, 700)", source)
        self.assertIn('fileNote:{filename, text:value}', source)
        self.assertIn('async function flushPendingNoteSaves()', source)
        self.assertIn('if (!await flushPendingNoteSaves()) return false;', source)
        dirty = source[source.index("function recomputeEditorDirty()") : source.index("function noteStatusElement")]
        self.assertNotIn("editorNotes", dirty)
        save = source[source.index("async function saveFileChanges") : source.index("function renderRunProjects")]
        self.assertNotIn("notes", save)

    def test_dependency_canvas_and_runtime_overflow_controls_are_present(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        for control in ("dependencyZoomOut", "dependencyZoomIn", "dependencyZoomActual", "dependencyZoomFit", "dependencySearch", "dependencyExportSvg", "dependencyExportPdf", "dependencyExportHtml"):
            self.assertIn(f'id="{control}"', markup)
        for control in ("dependencyFocusContext", "dependencyBreadcrumb", "dependencyScopeToggle"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn(".dependency-minimap", styles)
        self.assertIn("[data-dependency-scope]", source)
        self.assertIn("[data-dependency-view]", source)
        self.assertIn("dependencyNavigation", source)
        self.assertIn("graph.focusView?.metrics", source)
        self.assertIn("originId", source)
        self.assertIn("scope", source)
        self.assertIn("Export SVG", markup)
        self.assertIn("Export PDF", markup)
        self.assertIn("Export HTML", markup)
        self.assertLess(markup.index("dependency-search-row"), markup.index("dependency-zoom-row"))
        self.assertLess(markup.index("dependency-zoom-row"), markup.index("dependency-export-row"))
        self.assertIn(".dependency-zoom-row", styles)
        self.assertIn("dependency-export-row", markup)
        self.assertIn("editable SVG vector diagram", markup)
        self.assertIn("paginated PDF for printing or sharing", markup)
        self.assertIn("standalone HTML page", markup)
        self.assertIn('invoke("save_dependency_export"', source)
        self.assertNotIn('dependencyExportSvg").href', source)
        self.assertIn('saveDependencyExport("html")', source)
        self.assertIn("dependencyTemplateControl", markup)
        self.assertIn('VisionEval module catalog (built in)', source)
        self.assertIn("const hideTemplate = false", source)
        self.assertIn("template-hidden", source)
        self.assertLess(markup.index('id="dependencyMetrics"'), markup.index('class="panel dependency-toolbar"'))
        self.assertLess(markup.index('class="panel dependency-toolbar"'), markup.index('class="panel dependency-panel"'))
        self.assertIn("How produced traces upstream", markup)
        self.assertIn(".runtime-digest", styles)
        self.assertIn("overflow-x: hidden", styles)

    def test_run_queue_stop_all_controls_and_context_menu_are_present(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        self.assertIn("Choose a project to select baseline and scenarios.", markup)
        self.assertIn("run-empty-state", source)
        self.assertIn(".run-empty-state", styles)
        self.assertIn(".run-empty-state { display: none; }", styles)
        self.assertIn("openQueuedJobMenu", source)
        self.assertIn("No completed results are affected", source)
        self.assertIn("/api/runs/queue/remove", source)
        self.assertIn("Drag to reorder", source)
        self.assertIn("queue-drag-cue", source)
        self.assertNotIn('draggable="true"', source)
        self.assertIn('addEventListener("pointerdown"', source)
        self.assertIn('addEventListener("pointermove"', source)
        self.assertIn("queue-insertion-line", source)
        self.assertIn(".queue-insertion-line", styles)
        self.assertNotIn("data-queue-up", source)
        self.assertNotIn("data-queue-down", source)
        self.assertIn("Stop All Runs", source)
        self.assertIn("stopAllPending", source)
        self.assertIn("Stops only the selected active run", source)
        self.assertIn("Stops every active run", source)
        self.assertIn("followActiveConsoleJob", source)
        self.assertIn("consoleAutoFollowJob", source)
        self.assertIn("consoleManualSelection", source)
        self.assertIn("auto-following", styles)
        self.assertIn("/api/runs/stop-all", source)
        self.assertIn("stop-all-runs", source)
        self.assertIn("hasRunnableJobs", source)
        self.assertIn("stop-all-runs", rust)
        self.assertIn("has_runnable_jobs", rust)

    def test_run_batches_share_fifo_without_cross_batch_overlap_and_region_label_events_are_safe(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        runtime = (ROOT / "backend" / "workbench" / "runtime.py").read_text(encoding="utf-8")
        self.assertIn('id="queuedRunMode"', markup)
        self.assertIn("One container at a time across all projects", markup)
        self.assertIn("combined workspace queue", source)
        self.assertIn('$("parallelRunMode").hidden=native', source)
        self.assertIn("activeBatchId", runtime)
        self.assertIn("_active_batch_id_locked", runtime)
        self.assertIn("sharedOccupied instanceof Set", source)
        self.assertNotIn('addEventListener("change", updateRegionMapNameLabels)', source)
        self.assertIn('addEventListener("change", () => { updateRegionMapNameLabels(); updateRegionMapIdLabels(); })', source)

    def test_windows_native_runtime_forces_queued_runs(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        self.assertIn('id="parallelRunMode"', markup)
        self.assertIn('id="maxConcurrentRuns"', markup)
        self.assertIn('$("parallelRunMode").hidden=native', source)
        self.assertIn('$("defaultRunMode").disabled=native', source)
        self.assertIn('$("maxConcurrentRuns").value=String(native?1:(state.desktop.resources?.maxConcurrentRuns||2))', source)
        self.assertIn('runtime.adapter==="native"', source)
        self.assertIn('id="settingsVeHome"', markup)
        self.assertIn('id="settingsRscript"', markup)
        self.assertNotIn("ve-40-rc6-planrva-amd64", markup)
        self.assertIn('cfg!(target_os = "windows")', rust)
        self.assertIn('"native"', rust)
        self.assertIn('"docker"', rust)
        self.assertIn('profile.adapter == "native"', rust)
        self.assertIn("Stop All Runs", source)
        self.assertIn("Stopping All…", source)
        self.assertIn("stopAllPending", source)
        self.assertIn("Stops only the selected active run", source)
        self.assertIn("Stops every active run", source)
        self.assertIn("/api/runs/stop-all", source)
        self.assertIn("stop-all-runs", source)
        self.assertIn("hasRunnableJobs", source)
        self.assertIn("stop-all-runs", rust)
        self.assertIn("has_runnable_jobs", rust)
        self.assertIn("Stop This Hypercube", markup)
        self.assertIn("/api/hypercube-run/stop", source)
        self.assertIn("stopSelectedHypercube", source)

    def test_backend_lifecycle_serializes_restarts_and_uses_process_groups(self):
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        backend = (ROOT / "backend" / "main.py").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn("lifecycle: Mutex<()>", rust)
        self.assertIn("process_group(0)", rust)
        self.assertIn('api/backend/shutdown', rust)
        self.assertIn("WorkspaceLease", backend)
        self.assertIn("workspace is already open", backend)
        self.assertIn('/api/backend/shutdown', server)

    def test_generic_surfaces_do_not_use_optional_package_branding(self):
        script = ROOT / "packaging" / "check_generic_identity.py"
        spec = importlib.util.spec_from_file_location("check_generic_identity", script)
        module = importlib.util.module_from_spec(spec)
        self.assertIsNotNone(spec.loader)
        spec.loader.exec_module(module)
        self.assertEqual(module.violations(), [])

    def test_editor_sidebar_resizes_and_review_scenarios_collapse(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertNotIn('id="toggleScenarioSidebar"', markup)
        self.assertIn('id="scenarioSidebarResizer"', markup)
        self.assertIn('role="separator"', markup)
        self.assertIn("setScenarioSidebarWidth", source)
        self.assertIn("visioneval-scenario-sidebar-width", source)
        self.assertIn(".scenario-sidebar-resizer", styles)
        self.assertNotIn("sidebar-collapsed", styles)
        self.assertIn('<details class="review-scenario"', source)
        self.assertIn('<summary>', source)
        self.assertIn("reviewExpandedScenarioIds", source)
        self.assertIn(".review-scenario > summary", styles)
        self.assertIn(".change-editor-grid > label > input", styles)
        self.assertIn("height: 40px; min-height: 40px;", styles)

    def test_header_settings_gear_opens_settings(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn('id="settingsGear"', markup)
        self.assertIn('aria-label="Open settings"', markup)
        self.assertLess(markup.index('id="themeToggle"'), markup.index('id="settingsGear"'))
        self.assertIn('$("settingsGear").addEventListener("click",()=>openSettings());', source)

    def test_explore_library_copy_and_field_labels_are_clear(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        backend = (ROOT / "backend" / "workbench" / "explore.py").read_text(encoding="utf-8")
        self.assertNotIn(
            "Input explanations were developed for a Virginia dataset. They can still help interpret other VisionEval input files, but locally customized inputs may differ.",
            markup,
        )
        self.assertIn('id="exploreExplanationNotice" class="notice guidance-notice" hidden', markup)
        self.assertIn("function renderExploreExplanationNotice(packageMetadata = null)", source)
        self.assertIn("Boolean(state.exploreExplanationId)", source)
        self.assertIn('familyId === "virginia-visioneval-input-explanations"', source)
        self.assertIn(
            "Input explanations were developed for a Virginia dataset. They can still help interpret other VisionEval input files, but locally customized inputs may differ.",
            source,
        )
        self.assertIn("Fields in this file", source)
        self.assertNotIn("Variables in this file", source)
        self.assertIn("Not included", source)
        self.assertIn("This file is not included in the selected Input Library.", source)
        self.assertIn("No preloaded explanation is available for this input.", source)
        self.assertIn('detail.availability === "catalog_only"', source)
        self.assertNotIn('notify(error.message, "error");\n  }\n}\n\nfunction renderExploreDetail', source)
        self.assertIn("Description not available in the packaged guide.", backend)
        self.assertIn(".explore-toolbar input, .explore-toolbar select, .dependency-toolbar select", styles)
        self.assertIn(".dependency-toolbar button", styles)

    def test_input_explanations_replace_unit_model_control(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("Input explanations", markup)
        self.assertIn('id="exploreExplanations"', markup)
        self.assertNotIn("Unit model", markup)
        self.assertNotIn('id="exploreTemplate"', markup)
        self.assertNotIn('id="exploreLevel"', markup)
        self.assertIn("explanationPackageId", source)
        self.assertNotIn("exploreLevel", source)

    def test_settings_assets_use_installable_package_language(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("Install package…", markup)
        self.assertIn('id="settingsInstallPackage"', markup)
        self.assertIn('id="defaultInputExplanations"', markup)
        self.assertIn('id="installedAssetGroups"', markup)
        self.assertNotIn("Import an InputLibrary", markup)
        self.assertNotIn('id="settingsImportLibrary"', markup)
        self.assertNotIn("/api/setup/input-library", source)
        self.assertIn("Install a model package in Settings → Assets to begin.", markup)
        self.assertIn("Install a model package containing this file", source)
        self.assertNotIn('id="settingsImportTemplate"', markup)
        self.assertNotIn('id="templateImportForm"', markup)
        self.assertIn('id="settingsNumbers"', markup)
        for control in ("precisionDefault", "precisionSingleFile", "precisionBatch", "precisionOutput", "precisionPercentage"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn("Default decimal places for continuous values", markup)
        self.assertIn("VisionEval inputs are type-sensitive", markup)
        self.assertIn("These safeguards cannot be overridden", markup)
        self.assertIn("numericPrecision", source)
        self.assertIn('calculatedValue(next, "singleFile", state.csv, column)', source)
        self.assertIn("Verify runtime", markup)
        self.assertNotIn("Verify / Repair", markup)
        self.assertNotIn("after publication", markup)
        self.assertIn("Input explanations", source)
        self.assertIn('installSelectedPackage("choose_package")', source)
        self.assertIn("/api/packages/install", source)
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertNotIn('parsed.path == "/api/setup/input-library"', server)
        self.assertNotIn("Bundled PlanRVA MM example", markup)
        self.assertNotIn("installBundledPlanrva", source)

    def test_preference_reset_is_separate_from_workspace_reset(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        host = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        self.assertIn('id="resetPreferences"', markup)
        self.assertIn("Reset settings to defaults", markup)
        self.assertIn('invoke("reset_preferences")', source)
        self.assertIn('localStorage.removeItem("visioneval-app-zoom")', source)
        self.assertIn('"assetRegistrations": registrations', host)
        self.assertIn("reset_desktop_preference_fields", host)

    def test_settings_pages_use_balanced_top_aligned_spacing(self):
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn(".settings-page.active { display:grid; gap:14px; align-content:start; grid-auto-rows:max-content; }", styles)
        self.assertIn(".settings-page > button { justify-self:start; }", styles)
        self.assertIn(".documentation-library { display:grid; grid-template-columns:repeat(2,minmax(0,1fr));", styles)

    @unittest.skip("macOS-only desktop command contract")
    def test_macos_21_feedback_and_custom_region_contracts(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        config = (ROOT / "desktop" / "src-tauri" / "tauri.conf.json").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        self.assertIn('<html lang="en" data-theme="light"', markup)
        self.assertIn('storedTheme || "light"', source)
        self.assertIn('"light".into()', rust)
        self.assertIn('"resizable": true', config)
        self.assertIn('id="settingsNotice"', markup)
        self.assertIn('id="settingsNoticeAction"', markup)
        self.assertNotIn('applyComparisonPalettes();$("settingsDialog").close()', source)
        self.assertNotIn("Resource changes apply on the next app launch.", source)
        self.assertIn('notify("Settings saved and applied.","success")', source)
        self.assertIn('label:"Refresh Workbench"', source)
        self.assertIn('invoke("restart_backend")', source)
        self.assertIn('previousMemory!==nextMemory', source)
        self.assertIn('Docker startup preference will be used the next time Workbench opens.', source)
        self.assertIn('choose_package_folder', source)
        self.assertIn('async fn choose_package_folder', rust)
        self.assertIn('"choose_package_folder"', permissions)
        self.assertIn('id="choosePackageFolder"', markup)
        self.assertIn('regionBuilderIdentityDrafts', source)
        self.assertIn('Enter a region name before previewing.', source)
        self.assertNotIn('id="regionCode"', markup)
        self.assertNotIn('$("regionCode")', source)
        self.assertIn("automaticSummaryHeadline", source)
        self.assertIn('class="review-scenario-headline"', source)
        self.assertIn('class="automatic-summary-files"', source)
        self.assertIn("function renderAutomaticSummaryFile", source)
        self.assertIn('class="automatic-summary-locations"', source)
        self.assertIn("automaticSummarySearchText(file)", source)
        self.assertIn("expandedProjectIds: new Set()", source)
        self.assertIn("state.expandedProjectIds.has(project.id)", source)
        self.assertIn("displayLabel || item.label", source)
        self.assertIn("projectPackageName(project)", source)
        self.assertIn('id="editorNotesPanel"', markup)
        self.assertIn('id="scenarioNote"', markup)
        self.assertIn('id="fileNoteField"', markup)
        self.assertLess(markup.index('id="editorNotesPanel"'), markup.index('id="scenarioTools"'))
        self.assertNotIn("scenario-note-card", markup)
        self.assertIn('source:"batch"', source)
        self.assertIn('source:"single_file"', source)
        self.assertIn("batchId", source)
        self.assertIn('mixed:"Batch and single-file changes"', source)
        self.assertIn('if (!label) return "";', source)
        self.assertIn('role="img" aria-label=', source)
        self.assertIn("edit-source-marker", styles)
        self.assertIn("clip-path:polygon", styles)
        self.assertIn("grid-template-columns: minmax(0, 1fr) 34px 34px", styles)
        self.assertNotIn("edit-source-badge", styles)
        self.assertNotIn("PlanRVA Preview 2", markup)
        self.assertNotIn("preview-build-label", styles)
        self.assertNotIn("escapeHtml(record.templateId", source)
        self.assertIn('<svg viewBox="0 0 24 24"', source)
        self.assertIn('data-custom-region-svg', source)
        self.assertIn('updateCustomRegionMapView()', source)
        self.assertIn('[data-custom-region-mpos]', source)
        self.assertIn('requestAnimationFrame(() =>', source)
        custom_renderer = source[source.index("function renderRegionGeographySelectionMap") : source.index("function zoomCustomRegionMap")]
        self.assertEqual(2, custom_renderer.count("canvas.innerHTML ="))
        self.assertIn("if (rebuild || !svg || canvas.dataset.sceneKey !== sceneKey)", custom_renderer)
        self.assertLess(custom_renderer.index('data-custom-region-azones'), custom_renderer.index('data-custom-region-mpos'))
        pan_handler = source[source.index('window.addEventListener("pointermove",(event)=>') : source.index('window.addEventListener("pointerup",()=>')]
        self.assertIn("updateCustomRegionMapView()", pan_handler)
        self.assertNotIn("renderRegionGeographySelectionMap", pan_handler)
        self.assertIn('.region-builder-grid > label > input', styles)
        self.assertIn('height:40px; min-height:40px; box-sizing:border-box', styles)
        self.assertIn('id="settingsDocumentationLibrary"', markup)
        self.assertIn('id="documentationReaderDialog"', markup)
        self.assertIn('id="documentationReaderPages"', markup)
        self.assertIn('id="documentationReaderFit"', markup)
        self.assertIn('/vendor/pdfjs/pdf.min.mjs', source)
        self.assertIn('renderDocumentationPdf', source)
        self.assertIn('available/unscaled.width', source)
        self.assertTrue((ROOT / "public" / "vendor" / "pdfjs" / "pdf.worker.min.mjs").is_file())
        self.assertIn('Read in Workbench', source)
        self.assertIn('Open in Preview', markup)
        self.assertIn('/api/documentation/catalog', source)
        self.assertIn('/api/documentation/document?id=', source)
        self.assertIn('invoke("open_documentation_document"', source)
        self.assertNotIn('markdownLite', source)
        self.assertNotIn('id="settingsDocumentationPath"', markup)

    def test_workspace_setup_and_recent_workspace_management_are_explicit(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        setup = (ROOT / "desktop" / "src" / "index.html").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        self.assertNotIn('class="steps"', setup)
        self.assertIn("Change location…", setup)
        self.assertIn("Open existing workspace…", setup)
        self.assertIn('invoke("create_workspace"', setup)
        self.assertIn('invoke("choose_workspace_destination"', setup)
        self.assertIn('id="settingsOtherWorkspaces"', markup)
        self.assertIn(">Open another workspace<", markup)
        self.assertIn(">Move workspace<", markup)
        self.assertNotIn("Open another workspace…", markup)
        self.assertNotIn("Move workspace…", markup)
        self.assertIn('invoke("forget_workspace"', source)
        self.assertIn('invoke("trash_workspace"', source)
        self.assertIn(".workspace-recent-row", styles)
        for command in ("create_workspace", "choose_workspace_destination", "forget_workspace", "trash_workspace"):
            self.assertIn(f'"{command}"', permissions)
            self.assertIn(command, rust)

    def test_compare_uses_read_only_results_discovery_and_native_exports(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        for control in ("exportAllChangedCsv", "exportAllChangedWorkbook", "exportSelectedChangedCsv", "exportSelectedChangedWorkbook", "exportCurrentWorkbook", "exportFullVariablesZip", "exportFullVariablesWorkbook", "exportGeoControls", "fullExportVariableList", "scanScopeAll", "scanScopeSelected", "findChangedOutputs", "compareViewGeoControls", "compareGeoControls", "dashboardGeoControls", "dashboardDisplayMode", "dashboardHideZero", "exportDashboardCsv", "exportDashboardWorkbook"):
            self.assertIn(f'id="{control}"', markup)
        self.assertNotIn('id="datastoreImportForm"', markup)
        self.assertNotIn('id="showHiddenDatastores"', markup)
        self.assertNotIn("Data-path profiler", markup)
        self.assertNotIn("/api/datastores/import", source)
        self.assertNotIn("/api/datastores/hide", source)
        self.assertNotIn("/api/datastores/remove", source)
        self.assertNotIn("/api/comparison/profile-data-paths", source)
        self.assertIn("Previously registered", source)
        self.assertIn("Find All Changes", markup)
        self.assertIn("Export data", markup)
        self.assertIn("All Locations Changed Outputs", markup)
        self.assertIn("Selected Locations Changed Outputs", markup)
        self.assertIn("Full Variable Data", markup)
        self.assertIn("hideZero:String(settings.hideZero)", source)
        self.assertNotIn("Changed Rows in Current View", markup)
        self.assertNotIn("Reference Values", markup)
        self.assertNotIn("dashboardUseLocation", markup)
        self.assertIn("location-selector-trigger", styles)
        self.assertIn("dashboardDisplayRows", source)
        self.assertIn('state.compareLocationDirty=Boolean(state.lastComparison)', source)
        self.assertIn("scanFilterValues", source)
        self.assertIn('comparisonToken:state.lastComparison.comparisonToken', source)
        self.assertIn('marker=direction==="asc"?"↑":direction==="desc"?"↓":"---"', source)
        self.assertIn('aria-sort="${aria}"', source)
        self.assertIn("iqrComparison", source)
        self.assertIn("aggregate-average-change", source)
        self.assertIn("Average change", source)
        self.assertIn("change.measures?.mean?.percentChange", source)
        self.assertIn('phase==="preparing_cache"', source)
        self.assertIn("Preparing comparison cache", source)
        self.assertIn('id="notificationSuccessThreshold"', markup)
        self.assertIn("notificationSuccessThresholdSeconds", source)
        self.assertIn('force:true', source)
        self.assertIn("matched numeric rows", source)
        self.assertIn('id="compareResultsPanel"', markup)
        self.assertIn('id="comparisonResults"', markup)
        self.assertNotIn('id="comparisonSummary"', markup)
        self.assertIn('compareResultMode: "comparison"', source)
        self.assertIn('activateCompareResultMode("changedOutputs")', source)
        self.assertNotIn('header("Changed rows","changed")', source)
        self.assertNotIn('header("Total rows","total")', source)
        self.assertIn('class="iqr-values"', source)
        self.assertIn('id="toggleDashboardVariables"', markup)
        self.assertIn('id="dashboardVariableSearch"', markup)
        self.assertIn("setDashboardVariablesExpanded(false)", source)
        self.assertIn("updateDashboardVariableVisibility", source)
        self.assertIn(".dashboard-view-controls.has-display-value", styles)
        self.assertIn("comparisonValuesDiffer", source)
        self.assertIn('tr:not(.changed) td', styles)
        self.assertIn("At least ± threshold", markup)
        self.assertIn('metric("Not charted"', source)
        self.assertIn("/api/comparison/exports/start", source)
        self.assertIn('invoke("save_backend_export"', source)
        self.assertIn(".comparison-controls input, .comparison-controls select, .comparison-controls > button { height: 40px; }", styles)
        self.assertIn("user-select: none", styles)
        for item in ("export-dependency-svg", "export-current-csv", "export-all-changed-xlsx", "export-selected-changed", "export-full-variables", "export-dashboard-pdf", "export-dashboard-csv", "export-dashboard-xlsx"):
            self.assertIn(item, rust)
        for control in ("mapReference", "mapComparison", "mapYear", "mapTable", "mapVariable", "mapGeography", "mapAggregation", "generateMap", "mapMetric", "comparisonMapCanvas", "comparisonMapValueLabels", "toggleMapExport", "mapExportMenu", "exportMapPdf", "exportMapPng", "exportMapSvg", "exportMapCsv", "exportMapWorkbook"):
            self.assertIn(f'id="{control}"', markup)
        self.assertNotIn('id="comparisonMapRegion"', markup)
        self.assertIn('id="fitComparisonMap"', markup)
        self.assertIn('>Fit project<', markup)
        self.assertIn('comparison-map-detail-cards', source)
        self.assertIn('scaled to the largest visible absolute change', source)
        self.assertNotIn('colors clipped at the 95th percentile', source)
        self.assertIn('comparison-map-scale-axis', source)
        self.assertIn('comparisonMapMareaFeatures', source)
        self.assertIn('comparisonMapMareaVisuals', source)
        self.assertIn('comparisonMapMareaLabelAnchor', source)
        self.assertIn('comparisonMapStateBoundary', source)
        self.assertIn('comparisonMapDissolvedBoundary', source)
        self.assertIn('comparison-map-marea-outline', source)
        self.assertIn('comparison-marea-mask-', source)
        self.assertIn('.comparison-map-marea-hit { fill:transparent; stroke:none; pointer-events:fill; }', styles)
        self.assertIn('.comparison-map-value.marea-value { stroke:none; opacity:1; }', styles)
        self.assertIn('comparisonMapSvgGradientStops(scale)', source)
        self.assertIn('.28+.72*Math.sqrt(linear)', source)
        self.assertIn("scene.level==='marea'?[]:scene.project", source)
        self.assertIn("'fill-extrusion-opacity':1", source)
        self.assertIn('class="panel compare-unified-controls"', markup)
        self.assertNotIn('compare-selection-panel', markup)
        self.assertIn('.hypercube-activity-bar .activity-spinner,.hypercube-updating-overlay .activity-spinner{animation:activity-spin', styles)
        self.assertNotIn('border-radius:50%;animation:activity-spin', styles)
        self.assertLess(source.index('comparison-map-gradient diverging'), source.index('comparison-map-legend-keys'))
        for control in ("hideRunHistory", "showRunHistory", "compareMode", "appRecovery", "projectAssetIdentity"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn('data-subpage="mapData"', markup)
        self.assertIn("/api/comparison/options?view=", source)
        self.assertIn("comparisonOptions('map',ids)", source)
        self.assertIn("/api/comparison/operations/start", source)
        self.assertIn("full.width / 256", source)
        self.assertIn('table in {"Vehicle", "Worker"}', (ROOT / "backend" / "workbench" / "comparison.py").read_text(encoding="utf-8"))
        self.assertIn('f"{table}.HhId -> Household.HhId -> Household.Bzone"', (ROOT / "backend" / "workbench" / "comparison.py").read_text(encoding="utf-8"))
        self.assertIn("setComparisonMapExportOpen(false)", source)
        self.assertIn("comparisonMapSpatialIndex", source)
        self.assertIn("setApplicationZoom", source)
        self.assertIn('aggregation not in {"mean", "sum", "count"}', (ROOT / "backend" / "workbench" / "comparison.py").read_text(encoding="utf-8"))
        for item in ("export-map-pdf", "export-map-png", "export-map-svg", "export-map-csv", "export-map-xlsx", "save_visual_export", "zoom-in", "zoom-out", "actual-size", "map-zoom-in", "map-fit-mpo", "map-virginia", "set_app_zoom"):
            self.assertIn(item, rust)
        for removed in ("export-changed-xlsx", "export-scan-xlsx"):
            self.assertNotIn(removed, rust)
        self.assertNotIn("export-reference-csv", rust)

    def test_explicit_docker_start_and_safe_quit_commands_are_allowed(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        permissions = (ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        for control in ("startDockerDesktop", "onboardingStartDocker", "settingsStartDocker"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn('invoke("start_docker_desktop")', source)
        self.assertIn("invoke('complete_quit')", source)
        self.assertIn('"start_docker_desktop"', permissions)
        self.assertIn('"complete_quit"', permissions)
        self.assertIn('"save_dependency_export"', permissions)
        self.assertIn("fn start_docker_desktop()", rust)
        self.assertIn('"show-workspace-in-finder"', rust)
        self.assertIn('invoke("reveal_workspace_location"', source)
        self.assertIn("VE dependency {stamp}.{format}", rust)
        self.assertIn("async fn save_dependency_export(", rust)
        self.assertIn(".download_dir()", rust)
        self.assertNotIn("start_docker_desktop_if_requested", rust)

    @unittest.skip("Windows package does not build or ship a Docker runtime")
    def test_runtime_is_pinned_to_official_rc7_with_runtime_api_contract(self):
        dockerfile = (ROOT / "runtime" / "Dockerfile").read_text(encoding="utf-8")
        cli = (ROOT / "runtime" / "scripts" / "ve-cli.R").read_text(encoding="utf-8")
        self.assertIn("VISIONEVAL_REF=VE-40-RC7", dockerfile)
        self.assertIn("7852dc58fad460ff279f5eebf4dd55fe191470ad", dockerfile)
        self.assertIn('com.visioneval.upstream.release="VE-40-RC7"', dockerfile)
        self.assertIn('com.visioneval.workbench.runtime-api="1"', dockerfile)
        self.assertIn('com.visioneval.workbench.compatibility-patch="none"', dockerfile)
        self.assertNotIn("apply-household-id-prediction-ordering-patch.R", dockerfile)
        self.assertNotIn("PlanRVA", dockerfile)
        self.assertNotIn("PlanRVA", cli)
        self.assertIn('verify-upstream-release', cli)
        self.assertIn('verify-household-id-alignment', cli)
        self.assertFalse((ROOT / "runtime" / "patches" / "AlignPredictions.R").exists())
        self.assertFalse((ROOT / "runtime" / "scripts" / "apply-household-alignment-patch.R").exists())

    def test_stale_workspace_asset_defaults_fall_back_to_installed_assets(self):
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn("libraryIds.has(preferredLibrary)", source)
        self.assertIn('libraries.filter((item) => item.pairingStatus === "paired")', source)
        self.assertIn("eligibleLibraries.some((item) => item.id === preferredLibrary)", source)
        self.assertNotIn('$("templateSelect")', source)
        self.assertNotIn('$("defaultTemplate")', source)

    def test_windows_runtime_setup_is_version_independent_and_platform_aware(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        runtime = (ROOT / "backend" / "workbench" / "runtime.py").read_text(encoding="utf-8")
        cli = (ROOT / "runtime" / "scripts" / "ve-cli-native.R").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        for control in ("onboardingVeRuntime", "settingsVeRuntime", "settingsVeHome", "settingsRscript"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn('/api/runtime/discover', source)
        self.assertIn('CERTIFIED_R_VERSION = "4.5.3"', runtime)
        self.assertIn('RC7_RELEASE_COMMIT = "7852dc58fad460ff279f5eebf4dd55fe191470ad"', runtime)
        self.assertIn("native_runtime_provenance", runtime)
        self.assertIn("validate_native_path_separation", runtime)
        self.assertIn('veRuntimePath', source)
        self.assertIn('notificationsPlatformLabel', markup)
        self.assertIn('Windows notifications', source)
        self.assertIn('WORKBENCH_RUNTIME_INFO', cli)
        self.assertIn('checkModuleExists', cli)
        self.assertIn('verify-model', cli)
        self.assertNotIn('.tools\\VisionEval', runtime)
        self.assertIn('overflow-x: hidden', styles)
        self.assertIn('@media (max-width: 1100px)', styles)

    def test_region_builder_hides_statewide_and_matches_macos_map_label_defaults(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        labels = (ROOT / "public" / "polygon-labels.js").read_text(encoding="utf-8")
        self.assertIn('item.regionType !== "statewide"', source)
        self.assertIn('id="regionMapNameLabels" type="checkbox" checked', markup)
        self.assertIn('id="regionMapIdLabels" type="checkbox"> Zone ID labels', markup)
        self.assertIn("Selected MPO locality names", markup)
        self.assertIn("region?.azoneFips", source)
        self.assertIn("zoom >= 40", source)
        self.assertIn("zoom >= 1.75", source)
        self.assertIn('id="comparisonMapIdLabels" type="checkbox" checked', markup)
        for control in ("comparisonMap2d", "comparisonMap3d", "comparisonMap3dHeight", "comparisonMap3dCanvas", "resetComparisonMapBearing"):
            self.assertIn(f'id="{control}"', markup)
        for control in ("regionGeographyMap", "clearRegionGeography", "selectVisibleAzones"):
            self.assertIn(f'id="{control}"', markup)
        self.assertIn("regionMapInteriorLabel", source)
        self.assertIn("regionMapSignedDistance", source)
        self.assertIn('`url(#${clipId})`', labels)
        self.assertIn("initializeComparisonMap3dCapability", source)
        self.assertIn("renderComparisonMap3dBars", source)
        self.assertIn("Build your own region", markup)
        self.assertIn("Click Azones or Bzones", markup)
        self.assertIn("operationKind:'change-density'", source)
        self.assertIn('vendor/maplibre/maplibre-gl.css', markup)
        self.assertIn('scene.localityNames.get(String(id))', source)

    def test_custom_region_apply_preview_workflow_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="customGeographyHeading"', markup)
        for removed in ("regionGeographyMpo", "replaceRegionWithMpo", "addRegionMpo", "subtractRegionMpo"):
            self.assertNotIn(f'id="{removed}"', markup)
            self.assertNotIn(f'$("{removed}")', source)
        self.assertIn('id="customRegionMpoLayer"', markup)
        order = ["regionGeographySearch", "regionDisplayControls", "regionGeographyMap", "regionGeographyAzones"]
        self.assertEqual(order, sorted(order, key=lambda item: markup.index(f'id="{item}"')))
        self.assertIn('class="region-geography-map-overlay"', markup)
        self.assertNotIn('id="regionNavigationControls"', markup)
        self.assertIn('class="region-geography-footer"', markup)
        self.assertIn("position: sticky", styles)
        self.assertIn('$("applyRegionGeography").disabled = selected.size === 0', source)
        self.assertIn("new Set(state.regionBuilderDraftBzones)", source)
        self.assertIn("regionBuilderDraftInitialized", source)
        self.assertIn('switchCreateSubpage("createDevelop", false)', source)
        self.assertGreaterEqual(source.count("previewRegionBuild("), 3)
        self.assertIn("regionBuilderPreviewError", source)
        self.assertIn("Your geography selection is preserved", source)
        self.assertIn('state.regionBuilderGeographyMode === "custom"', source)
        self.assertIn('$("regionName").value.trim() || "Selected region"', source)

    def test_custom_region_independent_label_controls(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn('id="customRegionAzoneLabels" type="checkbox" checked', markup)
        self.assertIn('id="customRegionAzoneIdLabels" type="checkbox"', markup)
        self.assertIn('id="customRegionBzoneLabels" type="checkbox"', markup)
        self.assertIn('showAzoneNames && showAzoneIds', source)
        self.assertIn('[[name, id]', source)
        self.assertIn('"custom-region-azone-id-label"', source)
        self.assertNotIn("regionGeographyMpoBzones", source)
        self.assertNotIn("removed from official", source)

    def test_polygon_extrusions_and_palette_cards_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        smoke = (ROOT / "public" / "map3d-smoke.js").read_text(encoding="utf-8")
        self.assertIn("geometry:feature.geometry", source)
        self.assertNotIn("point.x-side", source)
        self.assertIn("directionVisible&&['increase','decrease'].includes(direction)?ratio*12000:0", source)
        self.assertIn("project:valueFeatures", source)
        self.assertIn("polygonExtrusion", smoke)
        self.assertIn("smoke-project-polygon", smoke)
        self.assertIn('class="muted palette-intro"', markup)
        self.assertIn("palette-color-control", source)
        self.assertIn("['decrease','neutral','increase']", source)
        self.assertIn("palette-color-copy", source)
        self.assertIn("linear-gradient", styles)
        self.assertIn("Choose color…", source)
        self.assertIn('aria-label="Choose ${title.toLowerCase()} color', source)
        self.assertIn(".palette-card-footer", styles)
        self.assertIn("container-name:palette-settings", styles)
        self.assertIn("container-type:inline-size", styles)
        self.assertIn(".palette-grid { display:grid; grid-template-columns:minmax(0,1fr)", styles)
        self.assertIn("@container palette-settings (min-width:1080px)", styles)
        self.assertIn("grid-template-columns:repeat(2,minmax(0,1fr))", styles)
        self.assertIn("@container palette-settings (max-width:640px)", styles)
        self.assertIn("overflow-x:hidden; overflow-y:auto", styles)
        self.assertIn("overflow-wrap:anywhere", styles)
        self.assertNotIn(".palette-card.palette-overridden { opacity", styles)

    def test_viewport_master_palette_and_advanced_3d_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        self.assertIn("--workbench-viewport-height", styles)
        self.assertIn("--workbench-dialog-edge-gap: 24px", styles)
        self.assertIn("window.visualViewport?.height", source)
        self.assertIn("--workbench-page-bottom-gutter", styles)
        self.assertIn("max-height: calc(var(--workbench-viewport-height)", styles)
        self.assertIn("settings-dialog-body", markup)
        self.assertIn("dialog-fixed-footer", markup)
        self.assertIn("Master palette", source)
        self.assertIn("useMasterComparisonPalette", source)
        self.assertIn("master_comparison_palette", rust)
        for control in ("comparisonMapProjectOnly", "comparisonMap3dElevationDirection", "comparisonMap3dAdvanced"):
            self.assertIn(f'id="{control}"', markup)
        self.assertNotIn("comparisonMap3dSmoothing", source)
        self.assertIn("WorkbenchExtrusionBorders", source)
        self.assertIn("NavigationControl", source)
        self.assertIn("preserveDrawingBuffer:true", source)
        self.assertNotIn("state.comparisonMap3d?.remove()", source)
        self.assertIn("canvas.toDataURL('image/png')", source)
        self.assertIn("grid-template-rows: auto auto auto minmax(520px, 68vh) auto auto", styles)
        self.assertIn("comparisonMap3dDefaultCamera", source)
        self.assertIn("comparisonMapOptionsInflight", source)
        self.assertIn("function comparisonMap3dVirginiaBounds", source)
        self.assertIn("scene.azones?.length?scene.azones:scene.bzones?.length?scene.bzones:scene.mpos", source)
        self.assertNotIn("state.comparisonMap3dScene?.context", source)

    def test_taskbar_safe_setup_export_and_signed_3d_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        setup = (ROOT / "desktop" / "src" / "index.html").read_text(encoding="utf-8")
        borders = (ROOT / "public" / "extrusion-borders.js").read_text(encoding="utf-8")
        self.assertIn("window.visualViewport?.height", setup)
        self.assertIn("--workbench-page-bottom-gutter: 96px", styles)
        self.assertIn(".taskbar-safe-dialog, .hypercube-safety-dialog, .region-geography-dialog, .region-map-dialog", styles)
        self.assertIn("position:fixed; inset:var(--workbench-dialog-edge-gap); margin:auto", styles)
        self.assertIn("max-width:calc(100vw - (2 * var(--workbench-dialog-edge-gap)))", styles)
        self.assertIn("max-height:calc(var(--workbench-viewport-height) - (2 * var(--workbench-dialog-edge-gap)))", styles)
        self.assertIn(".hypercube-safety-dialog { width: calc(100vw - (2 * var(--workbench-dialog-edge-gap)))", styles)
        self.assertIn("overflow:hidden; resize:none", styles)
        self.assertNotIn("--workbench-dialog-gutter", styles)
        self.assertNotIn("var(--workbench-viewport-height) - 112px", styles)
        self.assertNotIn("var(--workbench-viewport-height) - 72px", styles)
        self.assertIn("grid-template-rows: auto minmax(0,1fr) auto", styles)
        self.assertIn("container: comparison-map / inline-size", styles)
        self.assertIn("@container comparison-map (max-width: 1180px)", styles)
        self.assertIn(".comparison-map-presentation > * { min-width: 0; }", styles)
        self.assertNotIn('<option value="below">', markup)
        self.assertNotIn("zero-plane", source)
        self.assertNotIn("below zero", source.lower())
        self.assertIn("comparisonMap3dElevationDirection", source)
        self.assertIn("function comparisonMapSignedValue", source)
        self.assertIn("▼ Decrease", source)
        self.assertIn("▲ Increase", source)
        self.assertIn("decrease&&index%2===1", borders)

    def test_comparison_selector_context_values_and_elevated_border_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        borders = (ROOT / "public" / "extrusion-borders.js").read_text(encoding="utf-8")
        smoke = (ROOT / "public" / "map3d-smoke.js").read_text(encoding="utf-8")
        self.assertIn('role="radiogroup"', markup)
        self.assertIn('role="radio" aria-checked="true"', markup)
        self.assertIn("comparison-map-mode-slider", markup)
        self.assertIn("comparison-map-mode.is-3d", styles)
        self.assertIn("setAttribute('aria-checked'", source)
        self.assertIn("Azone context", markup)
        self.assertIn("Bzone context", markup)
        self.assertIn("sources:{azones:", source)
        self.assertIn("getSource('azones')", source)
        self.assertIn("getSource('bzones')", source)
        self.assertNotIn("values-line", source)
        self.assertIn("showNames=$('comparisonMapIdLabels').checked,showValues=$('comparisonMapValueLabels').checked", source)
        self.assertIn("__displayValue:displayValue,__heightValue:heightValue", source)
        self.assertIn("__color:color", source)
        self.assertIn("MultiPolygon", borders)
        self.assertIn("elevatedBorders", smoke)
        self.assertIn("borders.renderCount", smoke)
        self.assertIn('src="extrusion-borders.js"', markup)
        self.assertNotIn("earcut", markup.lower())

    def test_develop_mode_switch_and_setup_import_cleanup_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        self.assertIn('id="officialRegionGeographyView"', markup)
        self.assertIn('id="customRegionGeographyView"', markup)
        self.assertIn('id="editCustomRegionGeography"', markup)
        self.assertNotIn('id="setupPanel"', markup)
        self.assertNotIn('id="libraryImportForm"', markup)
        self.assertNotIn('id="templateImportForm"', markup)
        self.assertNotIn('$("customizeRegionGeography").addEventListener("click", openRegionGeographyDialog)', source)
        self.assertIn('$("editCustomRegionGeography").addEventListener("click", openRegionGeographyDialog)', source)

    def test_shared_polygon_label_engine_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        labels = (ROOT / "public" / "polygon-labels.js").read_text(encoding="utf-8")
        self.assertLess(markup.index('src="polygon-labels.js"'), markup.index('src="app.js"'))
        self.assertGreaterEqual(source.count("WorkbenchPolygonLabels.layout"), 2)
        for contract in ("MultiPolygon", "signedDistance", "getBoundingClientRect", "clipPath", "maxLabels", "priority"):
            self.assertIn(contract, labels)
        self.assertIn("shortenLocality", source)
        self.assertIn("devicePixelRatio", source)
        self.assertIn("ResizeObserver", source)

    def test_platform_shortcuts_and_accessibility_are_generated(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        rust = (ROOT / "desktop" / "src-tauri" / "src" / "main.rs").read_text(encoding="utf-8")
        self.assertIn('data-shortcut="save"', markup)
        self.assertNotIn("<kbd>⌘", markup)
        self.assertIn('other: "Ctrl+S"', source)
        self.assertIn('mac: "⌘S"', source)
        self.assertIn('setAttribute("aria-keyshortcuts"', source)
        self.assertIn("platform: String", rust)

    def test_deterministic_renderer_capability_and_packaging_contract(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        smoke = (ROOT / "public" / "map3d-smoke.js").read_text(encoding="utf-8")
        workflow = (ROOT.parent / ".github" / "workflows" / "release.yml").read_text(encoding="utf-8")
        self.assertIn('vendor/maplibre/maplibre-gl.js', markup)
        self.assertNotIn('type="module" src="map3d.js"', markup)
        self.assertFalse((ROOT / "public" / "map3d.js").exists())
        self.assertFalse((ROOT / "public" / "vendor" / "maplibre" / "maplibre-gl.mjs").exists())
        for capability in ("loading", "ready", "renderer-unavailable", "webgl-unavailable", "initialization-failed"):
            self.assertIn(f"'{capability}'", source)
        initializer = source[source.index("async function initializeComparisonMap3dCapability") :]
        self.assertLess(initializer.index("typeof window.maplibregl?.Map"), initializer.index("probeComparisonMapWebgl()"))
        for phase in ("renderer-assets", "renderer-global", "webgl", "empty-scene", "project-geojson", "fill-extrusion", "elevated-borders"):
            self.assertIn(phase, smoke)
        self.assertIn('src="extrusion-borders.js"', markup)
        self.assertIn("--comparison-map-3d enabled", workflow)
        self.assertIn("--bundles nsis", workflow)
        self.assertNotIn("docker build", workflow.lower())
        self.assertNotIn("workflow_call", workflow.lower())

    def test_desktop_bootstrap_commands_are_allowed_by_tauri_acl(self):
        setup = (ROOT / "desktop" / "src" / "index.html").read_text(encoding="utf-8")
        permission_path = ROOT / "desktop" / "src-tauri" / "permissions" / "workbench.toml"
        with permission_path.open("rb") as handle:
            permissions = tomllib.load(handle)
        allowed = set(permissions["permission"][0]["commands"]["allow"])
        invoked = set(re.findall(r'invoke\("([^"]+)"', setup))
        self.assertTrue(invoked)
        self.assertEqual(set(), invoked - allowed)
        self.assertIn("renderer_smoke_mode", allowed)
        self.assertIn("report_renderer_smoke", allowed)

    def test_hypercube_analysis_requires_explicit_aggregation_and_reuses_results(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        server = (ROOT / "backend" / "workbench" / "server.py").read_text(encoding="utf-8")
        self.assertIn('<option value="">Choose aggregation…</option>', markup)
        self.assertNotIn('<option value="auto">Auto</option>', markup)
        self.assertIn("No aggregation is universally correct", markup)
        self.assertIn('id="hypercubeDiscoveryYear"', markup)
        self.assertIn('id="hypercubeDiscoveryAggregation"', markup)
        self.assertIn('<option value="median" selected>Median</option>', markup)
        self.assertIn("function hypercubeDiscoveryRequest(){return {projectId:$('hypercubeAnalysisProject').value,year:$('hypercubeDiscoveryYear').value,aggregation:$('hypercubeDiscoveryAggregation').value||'median'};}", source)
        self.assertNotIn('id="hypercubeAnalysisCurveView"', markup)
        self.assertIn("restoreCachedHypercubeDiscovery", source)
        self.assertIn("Open Compare → Map with the common baseline", source)
        self.assertNotIn("if(selected.length===2)updateHypercubeAnalysis()", source)
        self.assertIn('/api/hypercube-analysis/discovery/cached', server)

    def test_find_all_changes_has_accessible_output_and_scenario_help(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        styles = (ROOT / "public" / "styles.css").read_text(encoding="utf-8")
        self.assertIn('id="hypercubeReadingGuideContent"', markup)
        self.assertIn('data-table-header-help', source)
        self.assertIn('aria-description=', source)
        self.assertIn("'Typical change':'Median absolute percentage deviation", source)
        self.assertIn("'Outputs changed':'Count and share of outputs", source)
        self.assertIn("'Excluded outputs':'Outputs omitted", source)
        self.assertIn("rankings measure sensitivity", source.lower())
        self.assertIn('.table-header-tooltip', styles)

    def test_frontend_event_bindings_reference_rendered_controls(self):
        markup = (ROOT / "public" / "index.html").read_text(encoding="utf-8")
        source = (ROOT / "public" / "app.js").read_text(encoding="utf-8")
        rendered_ids = set(re.findall(r'id="([^"]+)"', markup))
        dynamically_rendered_ids = set(re.findall(r'id=\\?"([^"]+)\\?"', source))
        directly_bound_ids = set(re.findall(r'\$\("([^"]+)"\)\.addEventListener', source))
        guarded_ids = set(re.findall(r'if\(\$\("([^"]+)"\)\)\s*\$\("\1"\)\.addEventListener', source))
        self.assertEqual(set(), directly_bound_ids - rendered_ids - dynamically_rendered_ids - guarded_ids)


if __name__ == "__main__":
    unittest.main()
