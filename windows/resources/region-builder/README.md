# Generic Region Builder scaffold

This directory contains the model-neutral VisionEval scaffold embedded in regional data packages. Region Builder writes the selected geography, inputs, identity, and metadata into a new template at build time.

Optional model packages maintain their own templates and provenance separately.
